---
description: Ask whether to use stacked PRs for a feature's tickets, estimate sizes, group tickets into batches, and annotate them with the metadata /stack-pr needs.
---
Load the `setup-stack` skill with the skill tool, then apply its workflow to the current situation. Do not look for it inside this repo — it is an installed skill, independent of the project.
