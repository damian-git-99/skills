# Local Overrides

## Research Sub-Agent

Use the dedicated `researcher` sub-agent to resolve every Research ticket. This replaces generic sub-agent dispatch or a `/research` sub-agent command.

When charting a map, start the `researcher` sub-agents for newly created Research tickets in parallel.
